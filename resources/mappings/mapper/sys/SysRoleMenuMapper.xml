<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 
【】对象关系映射 配置 
 @author AutoCode 309444359@qq.com
 @date 2017-10-17 16:44:07
 -->
<!-- namespace必须指向Dao接口 -->
<mapper namespace="com.bluemobi.dao.sys.SysRoleMenuDao">

	<resultMap id="sysRoleMenuResultMap" type="com.bluemobi.po.sys.SysRoleMenu">
       <result property="roleId" column="ROLE_ID" jdbcType="VARCHAR"/>
       <result property="menuId" column="MENU_ID" jdbcType="VARCHAR"/>
	</resultMap>
	<resultMap id="mapResultMap"  type="java.util.HashMap" extends="sysRoleMenuResultMap"></resultMap>
	<resultMap id="pageResultMap"  type="java.util.HashMap" extends="sysRoleMenuResultMap"></resultMap>
	
	<!-- 插入一条数据，需要在数据库中，创建表相关的序列，为了业务需求返回主键，不建触发器 -->
	<insert id="insert" parameterType="com.bluemobi.po.sys.SysRoleMenu">
		insert into SYS_ROLE_MENU (
			ROLE_ID,
			MENU_ID
		)values (
			 #{roleId,jdbcType=VARCHAR},
			 #{menuId,jdbcType=VARCHAR}
		)
	</insert>

	<!-- 根据主键查询 修改条目相关信息-->
	<update id="update" parameterType="com.bluemobi.po.sys.SysRoleMenu">
		update SYS_ROLE_MENU
		<set>
			<if test="roleId != null">
				ROLE_ID = #{roleId,jdbcType=VARCHAR},
			</if>
			<if test="menuId != null">
				MENU_ID = #{menuId,jdbcType=VARCHAR},
			</if>
		</set>
		<where>
		</where>
	</update>

	<!-- 根据表主键删除条目，确保逻辑删除字段为 is_del（0：未删除，1：已删除） -->
	<update id="delete" parameterType="map">
		update SYS_ROLE_MENU 
		SET
			is_del = 1	 
	   where 1=1
	</update>

	<!-- 根据主键查询 相关数据信息-->
	<select id="selectObject" parameterType="map" resultMap="sysRoleMenuResultMap">
		select 
		ROLE_ID,
		MENU_ID
		from SYS_ROLE_MENU
		where 
	</select>
	
	<!-- 根据相关字段 查询符合条件的数据信息 返回相关集合 -->
	<select id="selectObjectList" parameterType="map" resultMap="sysRoleMenuResultMap">
		select 
		ROLE_ID,
		MENU_ID
		from SYS_ROLE_MENU
	   <where>
	     <if test="roleId != null">
	        ROLE_ID=#{roleId}
	     </if>
	     <if test="menuId != null">
	       and MENU_ID=#{menuId}
	     </if>
	   </where>
	</select>

	<select id="selectMap" parameterType="map" resultMap="mapResultMap">
		select 
			ROLE_ID,
			MENU_ID
		from SYS_ROLE_MENU
		where 
	</select>

	<select id="selectMapList" parameterType="map" resultMap="mapResultMap">
		select 
	    ROLE_ID,
	    MENU_ID
		from SYS_ROLE_MENU
	   <where>
	     <if test="roleId != null">
	        ROLE_ID=#{roleId}
	     </if>
	     <if test="menuId != null">
	       and MENU_ID=#{menuId}
	     </if>
	   </where>
	</select>
	
	<select id="page" parameterType="map" resultMap="pageResultMap">
		select * from(
            select * from (
				select
				ROLE_ID,
				MENU_ID,
				rownum r
	       <include refid="pageCondition" />
	   <![CDATA[
	        )
	        where r <= #{offset}+#{rows}
	   )
       where r > #{offset}
       ]]>
	</select>

	<select id="pageCount" parameterType="map" resultType="int">
		select count(1)
	   <include refid="pageCondition" />
	</select>

	<sql id="pageCondition">
		from SYS_ROLE_MENU
		where 1 = 1
	     <if test="roleId != null">
			and ROLE_ID=#{roleId}
	     </if>
	     <if test="menuId != null">
			and MENU_ID=#{menuId}
	     </if>
	</sql>

	
</mapper> 
